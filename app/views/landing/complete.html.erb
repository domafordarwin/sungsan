<div class="survey-landing animate-in" style="text-align: center; padding-top: 4rem;">
  <div class="card-basalt" style="padding: 3rem;">
    <div style="font-size: 3rem; margin-bottom: 1rem;">&#9989;</div>
    <h2 style="font-family: var(--font-display); font-size: 1.25rem; margin-bottom: 0.75rem;">응답이 완료되었습니다</h2>
    <p style="color: var(--basalt-500); font-size: 0.875rem;">
      <strong><%= @survey.title %></strong>에 응답해 주셔서 감사합니다.
    </p>
    <% if @survey.event.present? %>
      <div class="card-basalt" style="padding: 1rem; margin-top: 1.5rem; text-align: left;">
        <span class="label-basalt">행사 정보</span>
        <p style="font-size: 0.875rem;">
          &#128197; <%= @survey.event.date.strftime("%Y년 %m월 %d일") %>
          <%= @survey.event.start_time.strftime("%H:%M") %>
          <% if @survey.event.location.present? %>
            <br>&#128205; <%= @survey.event.location %>
          <% end %>
        </p>
      </div>
    <% end %>
  </div>
</div>
