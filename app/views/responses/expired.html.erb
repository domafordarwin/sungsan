<div class="bg-white rounded-2xl shadow-lg p-8 text-center">
  <div class="text-4xl mb-4">&#x23f3;</div>
  <h1 class="text-xl font-bold text-gray-900 mb-2">응답할 수 없습니다</h1>
  <p class="text-gray-500 mb-6">
    <% if @assignment.responded_at.present? %>
      이미 응답이 완료된 배정입니다.
    <% else %>
      응답 기한이 만료되었습니다.
    <% end %>
  </p>
  <p class="text-sm text-gray-400">담당자에게 문의해 주세요.</p>
</div>
