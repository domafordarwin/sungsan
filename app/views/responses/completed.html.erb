<div class="bg-white rounded-2xl shadow-lg p-8 text-center">
  <% if @assignment.accepted? %>
    <div class="text-4xl mb-4">&#x2705;</div>
    <h1 class="text-xl font-bold text-green-700 mb-2">수락되었습니다</h1>
    <p class="text-gray-500 mb-6">봉사에 참여해 주셔서 감사합니다.</p>
  <% else %>
    <div class="text-4xl mb-4">&#x1f64f;</div>
    <h1 class="text-xl font-bold text-gray-700 mb-2">거절이 접수되었습니다</h1>
    <p class="text-gray-500 mb-6">다음에 봉사해 주세요.</p>
  <% end %>

  <div class="bg-gray-50 rounded-xl p-4 text-left text-sm space-y-1">
    <p><span class="text-gray-500">일정:</span> <%= @assignment.event.display_name %></p>
    <p><span class="text-gray-500">날짜:</span> <%= @assignment.event.date.strftime("%Y-%m-%d") %></p>
    <p><span class="text-gray-500">역할:</span> <%= @assignment.role.name %></p>
    <p><span class="text-gray-500">상태:</span>
      <span class="<%= @assignment.accepted? ? 'text-green-600' : 'text-red-600' %>">
        <%= @assignment.accepted? ? "수락" : "거절" %>
      </span>
    </p>
  </div>
</div>
