<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @survey&.title || "성산포 성당" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@400;600;700;900&display=swap" rel="stylesheet">
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="min-h-screen" style="background: var(--sand-cream);">
  <% if notice %>
    <div class="flash-notice animate-in"><%= notice %></div>
  <% end %>
  <% if alert %>
    <div class="flash-alert animate-in"><%= alert %></div>
  <% end %>

  <main class="landing-main">
    <%= yield %>
  </main>

  <footer style="text-align: center; padding: 2.5rem 1rem; font-size: 0.75rem; color: var(--basalt-400); border-top: 1px solid var(--basalt-200);">
    <span style="color: var(--sunrise-500);">&#10013;</span> 성산포 성당 제대봉사단
    <br>
    <span style="font-size: 0.65rem; margin-top: 0.25rem; display: inline-block;">AltarServe Manager</span>
  </footer>
</body>
</html>
