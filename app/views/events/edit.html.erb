<div class="max-w-2xl mx-auto">
  <div class="card-basalt animate-in" style="padding: 2rem;">
    <h1 style="font-family: var(--font-display); font-size: 1.25rem; font-weight: 700; margin-bottom: 1.5rem;">일정 수정</h1>
    <%= render "form", event: @event %>
  </div>
</div>
